const transaction_type = {
  DEPOSIT:'DEPOSIT',
  WITHDRAWAL:'WITHDRAWAL'
}

const LOG_ERR = {
  NO_TOKEN : "We don't have any porfolio for this token"
}
module.exports = LOG_ERR

module.exports = transaction_type;
